import React from "react";
import Row from "../Row";
import Col from "../Col";

export default function UserReports(props){
    return(
        <div>
            <h4>{props.report_date}</h4>
        </div>
    );
}